import { expect } from 'chai';
// import { SudokuSolver } from '../src/SudokuSolver';

describe('backtracking sudoku', () => {

  it('should make a new sudoku', function () {
    // let grid = SudokuSolver.generate();
    // console.log('grid', grid);/
    const grid = 1;
    expect(grid).not.equals(undefined);
  });

});